interface:
  display_name: "Travel Guide Publisher"
  short_description: "Build map guides with lodging and leave calendars"
  default_prompt: "Use $travel-guide-publisher to convert this travel article into an interactive guide with maps, lodging details, and a verified leave/holiday calendar, then publish it when requested."
